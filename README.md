# NodePerformanceTester
Using node v10.

You can see the multithread performance of node by `node hoge.js` and `ab -c 2 -n 2 localhost:3000/` or something

Testing web worker threads and cluster to improve performance.
